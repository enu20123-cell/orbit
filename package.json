{
  "name": "orbit-admission-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check src/app.js && node --check server/index.js && node build.mjs"
  }
}
